import type { Matrix } from 'pixi.js/lib/maths/matrix/Matrix';
import type { Batch, Batcher } from 'pixi.js/lib/rendering/batcher/shared/Batcher';
import type { DefaultBatchableMeshElement } from 'pixi.js/lib/rendering/batcher/shared/DefaultBatcher';
import type { Texture } from 'pixi.js/lib/rendering/renderers/shared/texture/Texture';
import type { ViewContainer } from 'pixi.js/lib/scene/view/View';
import type { MeshGeometry } from 'pixi.js/lib/scene/mesh/shared/MeshGeometry';
/**
 * A batchable mesh object.
 * @ignore
 */
export declare class BatchableMesh implements DefaultBatchableMeshElement {
    batcherName: string;
    readonly packAsQuad = false;
    location: number;
    renderable: ViewContainer;
    indexOffset: number;
    attributeOffset: number;
    texture: Texture;
    geometry: MeshGeometry;
    transform: Matrix;
    roundPixels: 0 | 1;
    _attributeStart: number;
    _batcher: Batcher;
    _batch: Batch;
    _indexStart: number;
    _textureId: number;
    private _transformedUvs;
    private _uvUpdateId;
    private _textureMatrixUpdateId;
    get blendMode(): import("pixi.js/lib").BLEND_MODES;
    reset(): void;
    get uvs(): Float32Array;
    get positions(): Float32Array;
    get indices(): Uint32Array;
    get color(): number;
    get groupTransform(): Matrix;
    get attributeSize(): number;
    get indexSize(): number;
}
