import { ExtensionType } from 'pixi.js/lib/extensions/Extensions';
import type { Geometry } from 'pixi.js/lib/rendering/renderers/shared/geometry/Geometry';
import type { Shader } from 'pixi.js/lib/rendering/renderers/shared/shader/Shader';
import type { Batch } from 'pixi.js/lib/rendering/batcher/shared/Batcher';
import type { BatcherAdaptor, BatcherPipe } from 'pixi.js/lib/rendering/batcher/shared/BatcherPipe';
/**
 * A BatcherAdaptor that uses WebGL to render batches.
 * @memberof rendering
 * @ignore
 */
export declare class GlBatchAdaptor implements BatcherAdaptor {
    /** @ignore */
    static extension: {
        readonly type: readonly [ExtensionType.WebGLPipesAdaptor];
        readonly name: "batch";
    };
    private _didUpload;
    private readonly _tempState;
    init(batcherPipe: BatcherPipe): void;
    contextChange(): void;
    start(batchPipe: BatcherPipe, geometry: Geometry, shader: Shader): void;
    execute(batchPipe: BatcherPipe, batch: Batch): void;
}
