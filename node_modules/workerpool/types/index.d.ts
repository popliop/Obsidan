export const Transfer: typeof import("workerpool/types/transfer.js");
export type Pool = import("workerpool/types/Pool.js");
export type WorkerPoolOptions = import("workerpool/types/types.js").WorkerPoolOptions;
export type WorkerRegisterOptions = import("workerpool/types/types.js").WorkerRegisterOptions;
/**
 * <T>
 */
export type Proxy<T extends {
    [k: string]: (...args: any[]) => any;
}> = import("workerpool/types/types.js").Proxy<T>;
/**
 * @overload
 * Create a new worker pool
 * @param {WorkerPoolOptions} [script]
 * @returns {Pool} pool
 */
export function pool(script?: import("workerpool/types/types.js").WorkerPoolOptions | undefined): import("workerpool/types/Pool.js");
/**
 * @overload
 * Create a new worker pool
 * @param {string} [script]
 * @param {WorkerPoolOptions} [options]
 * @returns {Pool} pool
 */
export function pool(script?: string | undefined, options?: import("workerpool/types/types.js").WorkerPoolOptions | undefined): import("workerpool/types/Pool.js");
/**
 * Create a worker and optionally register a set of methods to the worker.
 * @param {{ [k: string]: (...args: any[]) => any }} [methods]
 * @param {WorkerRegisterOptions} [options]
 */
export function worker(methods?: {
    [k: string]: (...args: any[]) => any;
} | undefined, options?: import("workerpool/types/types.js").WorkerRegisterOptions | undefined): void;
/**
 * Sends an event to the parent worker pool.
 * @param {any} payload
 */
export function workerEmit(payload: any): void;
import { Promise } from "workerpool/types/Promise.js";
import { platform } from "workerpool/types/environment.js";
import { isMainThread } from "workerpool/types/environment.js";
import { cpus } from "workerpool/types/environment.js";
export { Promise, platform, isMainThread, cpus };
